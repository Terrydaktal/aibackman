// Increment when the live ChatGPT cache representation changes in a way that
// requires every existing cached conversation to be downloaded again.
const STANDARD_CACHE_FORMAT_VERSION = 1;

module.exports = {
  STANDARD_CACHE_FORMAT_VERSION,
};
